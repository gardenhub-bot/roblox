======================================================================
  BASIT KURULUM: Administration Klasörü Yapısı
======================================================================

Bu dosya, admin panelini Administration klasöründe nasıl kuracağınızı
adım adım açıklar.

======================================================================
  ROBLOX STUDIO YAPILANMASI
======================================================================

ServerScriptService/
└── Administration/
    ├── AdminManager (ModuleScript)
    ├── AntiCheatSystem (ModuleScript)
    ├── EventLogger (ModuleScript)
    └── MainInit (Script)

ReplicatedStorage/
├── Modules/
│   └── DebugConfig (ModuleScript)
└── Remotes/
    ├── AdminCommand (RemoteEvent)
    ├── AdminDataUpdate (RemoteEvent)
    ├── EventLogUpdate (RemoteEvent)
    └── EventVFXTrigger (RemoteEvent)

StarterPlayer/
└── StarterPlayerScripts/
    └── AdminClient (LocalScript)

======================================================================
  ADIM 1: Administration Klasörünü Oluştur
======================================================================

1. ServerScriptService'i aç
2. "+" butonuna tıkla ve "Folder" seç
3. İsim: "Administration"

======================================================================
  ADIM 2: ModuleScript'leri Ekle
======================================================================

Administration klasörüne şunları ekle:

1. ADMINNMANAGER:
   - Insert Object → ModuleScript
   - İsim: "AdminManager"
   - GUNCEL_SCRIPTLER/AdminManager.lua kodunu kopyala

2. ANTICHEAT:
   - Insert Object → ModuleScript
   - İsim: "AntiCheatSystem"
   - GUNCEL_SCRIPTLER/AntiCheatSystem.lua kodunu kopyala

3. EVENTLOGGER:
   - Insert Object → ModuleScript
   - İsim: "EventLogger"
   - GUNCEL_SCRIPTLER/EventLogger.lua kodunu kopyala

4. MAININIT:
   - Insert Object → Script (NOT ModuleScript!)
   - İsim: "MainInit"
   - GUNCEL_SCRIPTLER/MainInitScript.lua kodunu kopyala

======================================================================
  ADIM 3: ReplicatedStorage Modülleri
======================================================================

1. ReplicatedStorage'da "Modules" klasörü oluştur (yoksa)
2. Modules içine:
   - ModuleScript ekle
   - İsim: "DebugConfig"
   - GUNCEL_SCRIPTLER/DebugConfig.lua kodunu kopyala

3. ReplicatedStorage'da "Remotes" klasörü oluştur
4. Remotes içine şunları ekle:
   - RemoteEvent, İsim: "AdminCommand"
   - RemoteEvent, İsim: "AdminDataUpdate"
   - RemoteEvent, İsim: "EventLogUpdate"
   - RemoteEvent, İsim: "EventVFXTrigger"

======================================================================
  ADIM 4: AdminClient (UI)
======================================================================

1. StarterPlayer → StarterPlayerScripts'i aç
2. LocalScript ekle
3. İsim: "AdminClient"
4. GUNCEL_SCRIPTLER/AdminClient.lua kodunu kopyala

======================================================================
  ADIM 5: Admin UserID Kontrol Et
======================================================================

AdminManager ModuleScript'ini aç ve kontrol et:

Satır 120 civarında:
```lua
Config.Admins = {
    [4221507527] = true,  -- ← Senin UserID'n burada olmalı!
}
```

Eğer farklıysa, kendi UserID'ni ekle.

======================================================================
  TEST
======================================================================

1. Play butonuna bas
2. Output penceresini aç (View → Output)
3. Şunları görmelisin:

✅ DebugConfig yüklendi
✅ AntiCheatSystem yüklendi
✅ EventLogger yüklendi
✅ AdminManager yüklendi
✅ Admin UserID: 4221507527
✅ Admin System Başarıyla Başlatıldı!
🎖️ Admin oyuncu katıldı: [İsmin] (UserID: 4221507527)
✅ IsAdmin attribute set edildi

4. Oyunda:
   - Sağ altta 🔧 butonu görünmeli
   - F2'ye basınca admin paneli açılmalı

======================================================================
  SORUN GİDERME
======================================================================

HATA: "AdminManager.Config.Admins bulunamadı!"
ÇÖZÜM:
  → AdminManager'ın Administration klasöründe olduğundan emin ol
  → AdminManager içindeki Config.Admins bölümünü kontrol et
  → GUNCEL_SCRIPTLER/AdminManager.lua dosyasını yeniden kopyala

HATA: "AntiCheatSystem yüklenemedi"
ÇÖZÜM:
  → AntiCheatSystem'in Administration klasöründe olduğundan emin ol
  → İsmin tam olarak "AntiCheatSystem" olmalı

HATA: "EventLogger yüklenemedi"
ÇÖZÜM:
  → EventLogger'ın Administration klasöründe olduğundan emin ol
  → İsmin tam olarak "EventLogger" olmalı

HATA: "Player is not an admin"
ÇÖZÜM:
  → AdminManager'daki Config.Admins'e UserID'ni ekle
  → UserID'nin doğru olduğundan emin ol
  → MainInit Script'in çalıştığından emin ol

PANEL AÇILMIYOR:
ÇÖZÜM:
  → Output'ta "Admin oyuncu katıldı" mesajını ara
  → Output'ta "IsAdmin attribute set edildi" mesajını ara
  → F2 tuşuna bas
  → Sağ alttaki 🔧 butonuna tıkla

======================================================================
  ÖZELLIKLER
======================================================================

Bu kurulum ile şunlar çalışır:

✅ 7 Event Sistemi (2xIQ, 2xCoins, Lucky Hour, vs.)
✅ Stat Yönetimi (IQ, Coins, Essence, Aura, RSToken, Rebirths)
✅ İksir Sistemi (Luck, IQ, Aura, Essence, Speed)
✅ Rot Skill Sistemi (RSToken, EquippedSkill)
✅ Event Bildirimleri (animasyonlu banner)
✅ Modern UI (6 sayfa: Dashboard, Stats, Potions, Rot Skills, Events, Logs)
✅ Anti-Cheat Sistemi
✅ Event Logging
✅ Rate Limiting (10 komut/60 saniye)
✅ Operation History (son 100 komut)

======================================================================
  SONUÇ
======================================================================

Her şey GUNCEL_SCRIPTLER klasöründe hazır ve çalışır durumda!

Sadece kopyala-yapıştır yap ve kullan!

İyi eğlenceler! 🎯✅
======================================================================
